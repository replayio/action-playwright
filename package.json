{
  "name": "action-playwright",
  "version": "0.5.6",
  "description": "Run and record Playwright tests with Replay",
  "main": "index.js",
  "scripts": {
    "tag": "GIT_TAG=$(node -e \"console.log('v'+require('./package.json').version)\"); test -z \"$(git tag -l $GIT_TAG)\" && npm i && git add . && git commit -m \"Publish $GIT_TAG\" && git tag $GIT_TAG && echo \"Tagged $GIT_TAG\" || echo \"Tag failed\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/recordreplay/action-playwright.git"
  },
  "author": "Replay.io",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/recordreplay/action-playwright/issues"
  },
  "homepage": "https://github.com/recordreplay/action-playwright#readme"
}
